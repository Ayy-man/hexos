---
phase: 15-meeting-assistant
plan: 07
type: execute
wave: 4
depends_on: ["15-04", "15-05", "15-06"]
files_modified:
  - features/meetings/components/task-list.tsx
  - features/meetings/components/task-row.tsx
  - features/meetings/components/task-import-dialog.tsx
  - features/meetings/components/task-export-button.tsx
  - features/meetings/components/convert-to-deliverable.tsx
  - features/meetings/actions/taskActions.ts
  - features/meetings/hooks/use-meetings-realtime.ts
  - features/projects/components/ProjectPageClient.tsx
autonomous: false

must_haves:
  truths:
    - "Meeting detail Tasks tab shows full task list with inline status toggling"
    - "Tasks can be created manually from the meeting detail page"
    - "Tasks can be exported as CSV from the meeting detail page"
    - "Tasks can be imported from CSV via upload dialog"
    - "A task can be converted to a project deliverable"
    - "Project detail page has a Meetings tab showing linked meetings"
    - "Meeting status updates appear in realtime"
  artifacts:
    - path: "features/meetings/components/task-list.tsx"
      provides: "Task table with filtering and bulk actions"
      min_lines: 60
    - path: "features/meetings/components/task-row.tsx"
      provides: "Single task row with inline editing"
      min_lines: 40
    - path: "features/meetings/components/task-import-dialog.tsx"
      provides: "CSV file upload dialog"
      min_lines: 40
    - path: "features/meetings/components/task-export-button.tsx"
      provides: "Button triggering CSV download"
      min_lines: 15
    - path: "features/meetings/components/convert-to-deliverable.tsx"
      provides: "One-click task to deliverable conversion"
      min_lines: 20
    - path: "features/meetings/actions/taskActions.ts"
      provides: "Server actions for task operations"
      exports: ["createTaskAction", "updateTaskAction", "deleteTaskAction", "convertToDeliverableAction"]
    - path: "features/meetings/hooks/use-meetings-realtime.ts"
      provides: "Supabase realtime subscription for meeting status"
      min_lines: 20
  key_links:
    - from: "features/meetings/components/task-list.tsx"
      to: "features/meetings/actions/taskActions.ts"
      via: "server action calls for CRUD"
      pattern: "createTaskAction|updateTaskAction|deleteTaskAction"
    - from: "features/meetings/components/task-export-button.tsx"
      to: "app/api/meeting-tasks/export/route.ts"
      via: "fetch CSV download URL"
      pattern: "/api/meeting-tasks/export"
    - from: "features/projects/components/ProjectPageClient.tsx"
      to: "lib/api/meetings.ts"
      via: "getMeetingsForEntity for project meetings tab"
      pattern: "getMeetingsForEntity|meetings"
---

<objective>
Build the task management UI (list, inline editing, CSV import/export, deliverable conversion), add realtime meeting status updates, and integrate a Meetings tab into the project detail page.

Purpose: Completes the meeting assistant feature by providing the task management interface, connecting meetings to projects via a dedicated tab, and adding realtime status updates so users can watch bot progress without refreshing.

Output: Task UI components, server actions, realtime hook, and project detail page integration.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/plans/2026-02-09-meeting-assistant-design.md
@.planning/phases/15-meeting-assistant/15-01-SUMMARY.md
@.planning/phases/15-meeting-assistant/15-04-SUMMARY.md
@.planning/phases/15-meeting-assistant/15-05-SUMMARY.md
@.planning/phases/15-meeting-assistant/15-06-SUMMARY.md
@features/projects/components/ProjectPageClient.tsx
@lib/api/meetings.ts
@lib/api/meeting-tasks.ts
@lib/types/meetings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create task management UI components and server actions</name>
  <files>features/meetings/components/task-list.tsx, features/meetings/components/task-row.tsx, features/meetings/components/task-import-dialog.tsx, features/meetings/components/task-export-button.tsx, features/meetings/components/convert-to-deliverable.tsx, features/meetings/actions/taskActions.ts</files>
  <action>
**features/meetings/actions/taskActions.ts** — Server actions with 'use server':

1. **createTaskAction(input: CreateMeetingTaskInput)** — Auth check, call createMeetingTask, revalidatePath, return {success, data?, error?}
2. **updateTaskAction(id: string, input: UpdateMeetingTaskInput)** — Call updateMeetingTask, revalidatePath, return {success, error?}
3. **deleteTaskAction(id: string)** — Call deleteMeetingTask, revalidatePath, return {success, error?}
4. **convertToDeliverableAction(taskId: string, projectId: string)** — Call convertTaskToDeliverable, revalidatePath, return {success, data?, error?}

**features/meetings/components/task-row.tsx** — Single task row:
- Props: task: MeetingTask, onUpdate, onDelete
- Inline status toggle: Checkbox that toggles between pending <-> done (calls updateTaskAction with status change)
- Display: title, assigned_to_name (or "Unassigned"), due_date (formatted, red if overdue), priority badge (color-coded: low=gray, normal=blue, high=amber, urgent=red)
- Source badge: small badge showing 'AI' for ai_extracted, 'Manual' for manual, 'Imported' for imported
- Actions dropdown (three-dot menu): Edit (expands inline form), Convert to Deliverable, Delete
- When "Convert to Deliverable" is clicked, render ConvertToDeliverable dialog

**features/meetings/components/task-list.tsx** — Task table:
- Props: tasks: MeetingTask[], meetingId: string
- Header row with columns: checkbox (for status toggle), Title, Assigned To, Due Date, Priority, Source, Actions
- Filter controls: status filter (All/Pending/In Progress/Done), priority filter
- "Add Task" button opens inline form (title input, optional fields) that calls createTaskAction
- Renders TaskRow for each task
- Bottom toolbar: "Import CSV" button (opens TaskImportDialog), "Export CSV" button (TaskExportButton)
- Empty state: "No tasks yet. Tasks will appear here after meeting processing, or you can add them manually."

**features/meetings/components/task-import-dialog.tsx** — CSV upload:
- Dialog with file input accepting .csv files
- On file select, preview file name and size
- Submit button calls `/api/meeting-tasks/import` with FormData (the CSV file)
- Include meetingId in query params to auto-link imported tasks
- Show result: "{imported} tasks imported, {skipped} skipped" with errors list if any
- On success, close dialog and refresh (router.refresh())
- Use sonner toast for success/error feedback

**features/meetings/components/task-export-button.tsx** — CSV download:
- Button labeled "Export CSV" with Download icon
- On click, construct URL: `/api/meeting-tasks/export?meeting_id={meetingId}`
- Trigger download: `window.open(url)` or create an anchor element and click it
- Simple, no dialog needed

**features/meetings/components/convert-to-deliverable.tsx** — Conversion dialog:
- Props: taskId: string, taskTitle: string, onConverted?: () => void
- Dialog asking which project to create the deliverable in
- Select dropdown with projects (fetch from API or pass as prop)
- On submit, call convertToDeliverableAction(taskId, projectId)
- Show success message with link to the project's deliverables
- If task already has deliverable_id, show "Already converted" message instead of form
  </action>
  <verify>
Verify all files exist:
- `test -f features/meetings/components/task-list.tsx && echo "OK"`
- `test -f features/meetings/components/task-row.tsx && echo "OK"`
- `test -f features/meetings/components/task-import-dialog.tsx && echo "OK"`
- `test -f features/meetings/components/task-export-button.tsx && echo "OK"`
- `test -f features/meetings/components/convert-to-deliverable.tsx && echo "OK"`
- `test -f features/meetings/actions/taskActions.ts && echo "OK"`
Verify server actions: `grep -c "'use server'" features/meetings/actions/taskActions.ts` should be 1
  </verify>
  <done>Task UI is complete: list with filtering, inline status toggle, add task form, CSV import dialog, CSV export button, and convert-to-deliverable dialog. Server actions handle all CRUD operations.</done>
</task>

<task type="auto">
  <name>Task 2: Add realtime hook and Meetings tab to project detail page</name>
  <files>features/meetings/hooks/use-meetings-realtime.ts, features/projects/components/ProjectPageClient.tsx</files>
  <action>
**features/meetings/hooks/use-meetings-realtime.ts** — Realtime subscription:
- Hook: `useMeetingsRealtime(meetingIds: string[])`
- Subscribe to Supabase realtime changes on 'meetings' table filtered by id in meetingIds
- On UPDATE event, return the updated meeting status
- Uses `useEffect` with cleanup (unsubscribe on unmount)
- Follow existing realtime patterns in the codebase (check for examples in features/notifications or features/conversations)
- Returns: { meetings: Meeting[] } with latest status for subscribed meetings

Alternative simpler approach: `useMeetingRealtime(meetingId: string)` for single meeting detail page — subscribe to changes on one meeting, update local state when status changes. This is simpler and more useful for the detail page.

**features/projects/components/ProjectPageClient.tsx** — Add Meetings tab:
- This is an existing file. Make MINIMAL changes.
- Add a new "Meetings" tab to the project detail tabs, positioned as the last content tab (before Settings if there is one, or at the end).
- The Meetings tab content should:
  - Accept projectId as prop
  - On tab activation (lazy load pattern), fetch meetings for this project using `getMeetingsForEntity('project', projectId)` via a client-side fetch to `/api/meetings?project_id={projectId}` or via a server action
  - Display a simple list of linked meetings: title, date, status badge, link to /meetings/[id]
  - "Link Meeting" button that opens a dialog to search existing meetings and link them (or just navigate to the meetings page for V1)
  - Empty state: "No meetings linked to this project."

IMPORTANT: Read the existing ProjectPageClient.tsx first to understand its tab structure. Add the new tab following the exact same pattern as existing tabs. The file likely uses a Tabs component with TabsTrigger and TabsContent. Add "Meetings" as a new entry.

Also wire up the MeetingDetail component (from plan 06) to include the TaskList (from this plan's Task 1) as the "Tasks" tab content. Update features/meetings/components/meeting-detail.tsx to import and render TaskList in the Tasks tab, passing the meeting's tasks.

Install dependencies if not already installed: `npm install papaparse svix && npm install -D @types/papaparse` (may already be done in plan 01 execution).
  </action>
  <verify>
Verify: `test -f features/meetings/hooks/use-meetings-realtime.ts && echo "OK"`
Verify: `grep -i "meeting" features/projects/components/ProjectPageClient.tsx | head -5` should show Meetings tab
Verify task list wired to detail: `grep "TaskList\|task-list" features/meetings/components/meeting-detail.tsx`
  </verify>
  <done>Realtime hook exists for meeting status updates. Project detail page has Meetings tab showing linked meetings. Meeting detail Tasks tab renders the full task management UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Meeting Assistant feature: meetings list page with create dialog, meeting detail with Summary/Tasks/Transcript/Recording/Participants tabs, task management with CSV import/export and deliverable conversion, Meetings tab on project detail, realtime status updates, sidebar navigation entry, and Recall.ai webhook integration.</what-built>
  <how-to-verify>
1. Navigate to /meetings — verify the page loads and shows "Meetings" in sidebar
2. Click "New Meeting" — verify dialog with title + URL fields appears
3. Verify meeting list shows columns: title, platform, date, status
4. Navigate to a meeting detail page (if test data exists) or verify /meetings/[id] route exists
5. On meeting detail, verify tabs: Summary, Tasks, Transcript, Recording, Participants
6. On Tasks tab, verify: add task button, CSV import/export buttons, task rows with status toggle
7. Navigate to a project detail page — verify "Meetings" tab appears
8. Check sidebar: Meetings entry visible for admin, not visible for non-admin roles
9. Verify build passes: `npm run build` completes without errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Task list renders with inline editing, status toggle, priority badges
2. CSV export triggers file download
3. CSV import accepts file and shows result summary
4. Convert to deliverable creates deliverable in target project
5. Project detail Meetings tab shows linked meetings
6. Realtime hook updates meeting status without refresh
7. Build completes without TypeScript errors
</verification>

<success_criteria>
- Full task management lifecycle works in meeting detail
- CSV import/export round-trips correctly
- Project detail shows linked meetings
- Realtime status updates work for active meetings
- All components integrate correctly — no broken imports or missing dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/15-meeting-assistant/15-07-SUMMARY.md`
</output>
