---
phase: 15-meeting-assistant
plan: 06
type: execute
wave: 3
depends_on: ["15-02", "15-03"]
files_modified:
  - app/(dashboard)/meetings/[id]/page.tsx
  - features/meetings/components/meeting-detail.tsx
  - features/meetings/components/meeting-summary.tsx
  - features/meetings/components/meeting-transcript.tsx
  - features/meetings/components/meeting-participants.tsx
  - features/meetings/components/meeting-link-picker.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can view full meeting detail at /meetings/[id]"
    - "Meeting detail shows tabs: Summary, Transcript, Recording, Participants"
    - "Summary tab shows AI-generated bullet summary and key decisions"
    - "Transcript tab shows diarized transcript with speaker labels and timestamps"
    - "Recording tab shows playback if recording URL exists"
    - "Participants tab shows list with profile matches"
    - "Admin can add/remove project/inquiry links from meeting detail"
  artifacts:
    - path: "app/(dashboard)/meetings/[id]/page.tsx"
      provides: "Server component for meeting detail page"
      min_lines: 20
    - path: "features/meetings/components/meeting-detail.tsx"
      provides: "Client component with tabs for meeting detail"
      min_lines: 80
    - path: "features/meetings/components/meeting-summary.tsx"
      provides: "Summary + key decisions display"
      min_lines: 30
    - path: "features/meetings/components/meeting-transcript.tsx"
      provides: "Diarized transcript viewer with search"
      min_lines: 50
    - path: "features/meetings/components/meeting-link-picker.tsx"
      provides: "Dialog to link projects/inquiries to meeting"
      min_lines: 40
  key_links:
    - from: "app/(dashboard)/meetings/[id]/page.tsx"
      to: "lib/api/meetings.ts"
      via: "getMeeting server fetch"
      pattern: "import.*getMeeting.*from.*api/meetings"
    - from: "features/meetings/components/meeting-detail.tsx"
      to: "features/meetings/components/meeting-summary.tsx"
      via: "renders summary tab content"
      pattern: "import.*MeetingSummary"
    - from: "features/meetings/components/meeting-detail.tsx"
      to: "features/meetings/components/meeting-transcript.tsx"
      via: "renders transcript tab content"
      pattern: "import.*MeetingTranscript"
---

<objective>
Build the meeting detail page with tabbed interface showing AI summary, diarized transcript, recording playback, participant list, and project/inquiry linking.

Purpose: The detail page is where admins review AI-generated meeting notes, browse the transcript, and manage meeting-project relationships. This is the primary consumption interface for processed meeting data.

Output: Meeting detail page at /meetings/[id] with 4 tabs and link management.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/plans/2026-02-09-meeting-assistant-design.md
@.planning/phases/15-meeting-assistant/15-01-SUMMARY.md
@.planning/phases/15-meeting-assistant/15-02-SUMMARY.md
@.planning/phases/15-meeting-assistant/15-03-SUMMARY.md
@lib/api/meetings.ts
@lib/types/meetings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create meeting detail page and main detail component</name>
  <files>app/(dashboard)/meetings/[id]/page.tsx, features/meetings/components/meeting-detail.tsx</files>
  <action>
**app/(dashboard)/meetings/[id]/page.tsx** — Server component:
- requireAuth(), getProfile(), verify admin role
- Extract id from params (follow `app/(dashboard)/projects/[id]/page.tsx` pattern with `params: Promise<{ id: string }>`)
- Call getMeeting(id) from lib/api/meetings.ts
- If null, call notFound()
- Render MeetingDetail with meeting data and profile info
- `export const dynamic = 'force-dynamic'`

**features/meetings/components/meeting-detail.tsx** — Client component with tabs:
- Props: meeting: MeetingWithLinks, userRole: string
- Header section:
  - Meeting title (editable inline if needed, or just display)
  - MeetingStatusBadge (reuse from plan 05)
  - Platform label, date, duration (formatted from duration_seconds: "1h 23m")
  - Linked entities as clickable chips/badges (project name links to /projects/[id], inquiry links to /inquiries/[id])
  - "Link to..." button that opens MeetingLinkPicker
- Use Tabs/TabsList/TabsTrigger/TabsContent from shadcn
- Tab list: Summary, Transcript, Recording, Participants
- Summary tab: Render MeetingSummary component
- Transcript tab: Render MeetingTranscript component
- Recording tab: If meeting.recall_recording_url exists, render an audio/video element with the URL. If not, show "Recording not available" placeholder.
- Participants tab: Render MeetingParticipants component
- If meeting.status !== 'ready', show a status message instead of tabs: "Meeting is currently {status}..." with appropriate messaging for each non-ready status (pending = "Waiting for bot to join", joining = "Bot is joining the meeting", recording = "Meeting in progress", processing = "AI is processing the transcript", failed = "Processing failed: {error_message}")

Use the lazy tab loading pattern from the codebase: load tab content only when tab is activated.
  </action>
  <verify>
Verify files exist:
- `test -f app/\(dashboard\)/meetings/\[id\]/page.tsx && echo "OK"`
- `test -f features/meetings/components/meeting-detail.tsx && echo "OK"`
Verify tabs: `grep -c "TabsTrigger" features/meetings/components/meeting-detail.tsx` should be >= 4
  </verify>
  <done>Meeting detail page renders at /meetings/[id] with header showing status/platform/links and tabbed content for Summary, Transcript, Recording, Participants.</done>
</task>

<task type="auto">
  <name>Task 2: Create Summary, Transcript, Participants, and Link Picker components</name>
  <files>features/meetings/components/meeting-summary.tsx, features/meetings/components/meeting-transcript.tsx, features/meetings/components/meeting-participants.tsx, features/meetings/components/meeting-link-picker.tsx</files>
  <action>
**features/meetings/components/meeting-summary.tsx**:
- Props: summary: string | null, keyDecisions: KeyDecision[] | null
- If summary exists, render as markdown bullet list (the summary is stored as markdown text with bullet points)
- Key Decisions section: render each as a card with "Decision" heading and "Context" subtext
- If no summary, show "Summary not available yet" placeholder
- Simple, clean layout — no complex interactions

**features/meetings/components/meeting-transcript.tsx**:
- Props: transcript: TranscriptSegment[] | null
- Search input at top to filter segments by text content
- Render each segment as a row:
  - Speaker name (bold, left-aligned)
  - Timestamp (formatted from seconds: "2:05" or "1:23:05")
  - Text content
- Different background color for alternating speakers
- Client-side search: filter segments where text includes search term (case-insensitive)
- If no transcript, show "Transcript not available" placeholder
- Format timestamp helper: convert seconds to MM:SS or H:MM:SS

**features/meetings/components/meeting-participants.tsx**:
- Props: participants: MeetingParticipant[]
- Simple list/table with columns: Name, Email, Speaker Label, Hexos Profile
- If profile_id exists, show profile name as a link to their profile
- If no profile match, show "External" badge
- If no participants, show "No participants recorded"

**features/meetings/components/meeting-link-picker.tsx**:
- Props: meetingId: string, existingLinks: MeetingLink[], onLinkAdded?: () => void
- Dialog with a dropdown to select linkable_type (Project / Inquiry)
- When type is selected, fetch the list of projects or inquiries (use existing API helpers: getProjects, getInquiries from lib/api/)
- Show a searchable select/combobox to pick the entity
- On submit, call addMeetingLink server action
- Show existing links as removable chips
- Removing a chip calls removeMeetingLink server action

Create server actions for link management in features/meetings/actions/meetingActions.ts (add to the file created in plan 05):
- **addMeetingLinkAction(meetingId, linkableType, linkableId)** — Call addMeetingLink from lib/api/meetings.ts, revalidatePath
- **removeMeetingLinkAction(linkId)** — Call removeMeetingLink from lib/api/meetings.ts, revalidatePath

Import shadcn components: Dialog, Select, Command (for combobox), Badge, Button, Input.
  </action>
  <verify>
Verify all files exist:
- `test -f features/meetings/components/meeting-summary.tsx && echo "OK"`
- `test -f features/meetings/components/meeting-transcript.tsx && echo "OK"`
- `test -f features/meetings/components/meeting-participants.tsx && echo "OK"`
- `test -f features/meetings/components/meeting-link-picker.tsx && echo "OK"`
Verify transcript search: `grep -i "search\|filter" features/meetings/components/meeting-transcript.tsx`
Verify link picker dialog: `grep "Dialog" features/meetings/components/meeting-link-picker.tsx`
  </verify>
  <done>Four components exist: MeetingSummary (bullets + decisions), MeetingTranscript (diarized viewer with search), MeetingParticipants (list with profile links), MeetingLinkPicker (add/remove project/inquiry links).</done>
</task>

</tasks>

<verification>
1. Meeting detail page loads at /meetings/[id] for admin users
2. Summary tab shows AI bullet points and key decisions
3. Transcript shows speaker-labeled segments with timestamps and search
4. Recording tab shows playback or "not available" message
5. Participants list shows profiles and external markers
6. Link picker allows adding/removing project/inquiry links
7. Non-ready meetings show appropriate status messaging
</verification>

<success_criteria>
- All 4 tabs render correctly with meeting data
- Transcript search filters segments in real time
- Link picker searches and selects projects/inquiries
- Status-dependent messaging guides user appropriately
</success_criteria>

<output>
After completion, create `.planning/phases/15-meeting-assistant/15-06-SUMMARY.md`
</output>
