---
phase: 15-meeting-assistant
plan: 05
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - app/(dashboard)/meetings/page.tsx
  - features/meetings/components/meeting-list.tsx
  - features/meetings/components/meeting-status-badge.tsx
  - features/meetings/components/new-meeting-dialog.tsx
  - features/meetings/actions/meetingActions.ts
  - lib/navigation.ts
  - components/app-sidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can navigate to /meetings from the sidebar"
    - "Meeting list page shows all meetings sorted by date with status badges"
    - "Admin can filter meetings by status"
    - "Admin can create a new meeting by pasting a link and providing a title"
    - "New meeting dialog dispatches bot and shows confirmation"
    - "Meetings sidebar entry is admin-only"
  artifacts:
    - path: "app/(dashboard)/meetings/page.tsx"
      provides: "Server component for /meetings page"
      min_lines: 15
    - path: "features/meetings/components/meeting-list.tsx"
      provides: "Client component showing meetings table/list"
      min_lines: 50
    - path: "features/meetings/components/new-meeting-dialog.tsx"
      provides: "Dialog for creating a new meeting"
      min_lines: 40
    - path: "features/meetings/actions/meetingActions.ts"
      provides: "Server actions for meeting operations"
      exports: ["createMeetingAction", "deleteMeetingAction"]
    - path: "lib/navigation.ts"
      provides: "Updated sidebar navigation with Meetings entry"
      contains: "Meetings"
  key_links:
    - from: "features/meetings/components/meeting-list.tsx"
      to: "features/meetings/actions/meetingActions.ts"
      via: "server action calls"
      pattern: "createMeetingAction|deleteMeetingAction"
    - from: "app/(dashboard)/meetings/page.tsx"
      to: "lib/api/meetings.ts"
      via: "server-side data fetch"
      pattern: "import.*getMeetings.*from.*api/meetings"
    - from: "lib/navigation.ts"
      to: "app/(dashboard)/meetings/page.tsx"
      via: "sidebar link to /meetings"
      pattern: "url.*meetings"
---

<objective>
Build the meetings list page, new meeting creation dialog, server actions, and add the Meetings entry to the admin sidebar navigation.

Purpose: This is the primary entry point for the meeting assistant feature. Users create meetings from here, see all meetings at a glance, and navigate to meeting details.

Output: Fully functional /meetings page with create dialog, status badges, and sidebar navigation entry.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/plans/2026-02-09-meeting-assistant-design.md
@.planning/phases/15-meeting-assistant/15-01-SUMMARY.md
@.planning/phases/15-meeting-assistant/15-02-SUMMARY.md
@lib/navigation.ts
@components/app-sidebar.tsx
@app/(dashboard)/projects/[id]/page.tsx
@lib/api/meetings.ts
@lib/types/meetings.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create meetings list page and components</name>
  <files>app/(dashboard)/meetings/page.tsx, features/meetings/components/meeting-list.tsx, features/meetings/components/meeting-status-badge.tsx, features/meetings/components/new-meeting-dialog.tsx, features/meetings/actions/meetingActions.ts</files>
  <action>
**features/meetings/actions/meetingActions.ts** — Server actions following structured result pattern:

1. **createMeetingAction(input: CreateMeetingInput)** — Get authenticated user, call createMeeting from lib/api/meetings.ts, revalidatePath('/meetings'), return {success, data?, error?}.
2. **deleteMeetingAction(id: string)** — Call deleteMeeting, revalidatePath('/meetings'), return {success, error?}.

Use `'use server'` directive. Import auth check from `@/lib/auth/guards` (requireAuth/getProfile pattern used elsewhere).

**features/meetings/components/meeting-status-badge.tsx** — Small component:
- Map MeetingStatus to Badge variant and label:
  - pending: secondary, "Pending"
  - joining: secondary, "Joining..."
  - recording: destructive (red pulse), "Recording"
  - processing: secondary, "Processing..."
  - ready: default (green), "Ready"
  - failed: destructive, "Failed"
- Use Badge from @/components/ui/badge
- Add a subtle animation for 'recording' status (animate-pulse on a red dot)

**features/meetings/components/new-meeting-dialog.tsx** — Dialog component:
- Uses Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger from shadcn
- Form fields: title (text input, required), meeting_url (text input with placeholder "Paste Zoom, Meet, or Teams link", required)
- Optional: link to project/inquiry via a simple select (can be empty for V1)
- Submit calls createMeetingAction
- On success: close dialog, show success toast via sonner
- On error: show error toast
- Button triggers dialog: "New Meeting" with Plus icon

**features/meetings/components/meeting-list.tsx** — Client component:
- Props: meetings: Meeting[], initialStatus?: string
- Table layout with columns: Title (link to /meetings/[id]), Platform (icon for zoom/meet/teams), Date (formatted created_at), Duration (formatted or "-"), Status (MeetingStatusBadge), Actions (delete button in dropdown)
- Platform icons: use simple text labels or emoji for V1 (Zoom, Meet, Teams, Other)
- Status filter: tabs or select dropdown for status filtering (All, Pending, Recording, Processing, Ready, Failed)
- Client-side filtering is OK for V1 since data is pre-fetched
- Empty state: "No meetings yet. Create your first meeting to get started."
- Each row title links to /meetings/[id]

**app/(dashboard)/meetings/page.tsx** — Server component:
- requireAuth() guard
- getProfile() to verify admin role, redirect if not admin
- getMeetings() to fetch all meetings
- Render page header with title "Meetings" and NewMeetingDialog button
- Render MeetingList with meetings data
- Use `export const dynamic = 'force-dynamic'` (matching project detail pattern)
  </action>
  <verify>
Verify all files exist:
- `test -f app/\(dashboard\)/meetings/page.tsx && echo "OK"`
- `test -f features/meetings/components/meeting-list.tsx && echo "OK"`
- `test -f features/meetings/components/meeting-status-badge.tsx && echo "OK"`
- `test -f features/meetings/components/new-meeting-dialog.tsx && echo "OK"`
- `test -f features/meetings/actions/meetingActions.ts && echo "OK"`
Run: `npx tsc --noEmit 2>&1 | grep "meetings" | head -10` for type check
  </verify>
  <done>Meetings page renders at /meetings with table of meetings, status badges, new meeting dialog with create action, and delete capability.</done>
</task>

<task type="auto">
  <name>Task 2: Add Meetings to admin sidebar navigation</name>
  <files>lib/navigation.ts, components/app-sidebar.tsx</files>
  <action>
**lib/navigation.ts**:
1. Add "Meetings" to the adminNav 'Management' group, after 'Conversations':
   ```
   { title: 'Meetings', url: '/meetings', icon: 'Video' }
   ```
2. Also add to internalNav 'Management' group at the same position.
3. Do NOT add to devNav, dfyNav, or clientNav (admin-only V1).

**components/app-sidebar.tsx**:
1. Import `Video` from 'lucide-react' (add to the existing import)
2. Add 'Video' to the iconMap object: `Video,`

These are small, surgical edits to existing files. Keep all existing navigation items unchanged.
  </action>
  <verify>
Verify: `grep "Meetings" lib/navigation.ts` should match
Verify: `grep "Video" components/app-sidebar.tsx` should match
Verify: `grep "Video" lib/navigation.ts` should match the icon name
Verify meetings URL: `grep "/meetings" lib/navigation.ts` should match
  </verify>
  <done>Meetings appears in admin and internal sidebar navigation with Video icon, linking to /meetings.</done>
</task>

</tasks>

<verification>
1. /meetings page loads for admin users
2. New Meeting dialog creates meeting and dispatches bot
3. Meeting list shows all meetings with status badges
4. Sidebar shows "Meetings" link for admin and internal roles only
5. Meeting titles link to /meetings/[id] (detail page built in next plan)
6. Delete action works and removes meeting
</verification>

<success_criteria>
- Meetings page accessible at /meetings for admin users
- New meeting creation works end-to-end (form -> action -> API -> Recall.ai)
- Meeting list shows all fields (title, platform, date, status)
- Sidebar navigation updated for admin and internal roles
</success_criteria>

<output>
After completion, create `.planning/phases/15-meeting-assistant/15-05-SUMMARY.md`
</output>
