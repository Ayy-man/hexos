---
phase: 05-sidebar-dashboard-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/navigation.ts
autonomous: true

must_haves:
  truths:
    - "Blockers appears first in Admin group for admin users"
    - "Blockers appears first in Admin group for internal users"
    - "Other navigation items remain in their original groups"
  artifacts:
    - path: "lib/navigation.ts"
      provides: "Navigation configuration with reordered Admin group"
      contains: "{ title: 'Blockers'"
  key_links:
    - from: "lib/navigation.ts"
      to: "components/app-sidebar.tsx"
      via: "getNavigation() function"
      pattern: "getNavigation\\(.*role\\)"
---

<objective>
Reorder sidebar navigation to place Blockers first in Admin group

Purpose: Blockers require immediate attention - placing them higher in the Admin group ensures visibility
Output: Updated navigation.ts with Blockers promoted to first position in Admin group for both adminNav and internalNav
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-sidebar-dashboard-polish/05-RESEARCH.md
@lib/navigation.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reorder Admin group in adminNav and internalNav</name>
  <files>lib/navigation.ts</files>
  <action>
In `lib/navigation.ts`, update both `adminNav` and `internalNav` arrays to move Blockers to the first position in the Admin group.

Current Admin group order (lines 37-46 in adminNav, lines 83-91 in internalNav):
```typescript
{
  label: 'Admin',
  items: [
    { title: 'Metrics', url: '/dashboard/admin/metrics', icon: 'BarChart3' },
    { title: 'Finances', url: '/finances', icon: 'DollarSign' },
    { title: 'Time Reports', url: '/admin/time-reports', icon: 'Clock' },
    { title: 'Blockers', url: '/admin/blockers', icon: 'AlertTriangle' },
    { title: 'Developers', url: '/admin/devs', icon: 'Users' },
    { title: 'Opportunities', url: '/admin/opportunities', icon: 'Briefcase' },
  ],
}
```

New Admin group order (Blockers first):
```typescript
{
  label: 'Admin',
  items: [
    { title: 'Blockers', url: '/admin/blockers', icon: 'AlertTriangle' },
    { title: 'Metrics', url: '/dashboard/admin/metrics', icon: 'BarChart3' },
    { title: 'Finances', url: '/finances', icon: 'DollarSign' },
    { title: 'Time Reports', url: '/admin/time-reports', icon: 'Clock' },
    { title: 'Developers', url: '/admin/devs', icon: 'Users' },
    { title: 'Opportunities', url: '/admin/opportunities', icon: 'Briefcase' },
  ],
}
```

Apply this change to BOTH:
1. `adminNav` array (around line 37)
2. `internalNav` array (around line 83)

Do NOT modify any other navigation arrays (devNav, dfyNav, clientNav).
  </action>
  <verify>
1. Run `grep -A1 "label: 'Admin'" lib/navigation.ts` to confirm Blockers is first item after Admin label
2. Run TypeScript check: `npx tsc --noEmit lib/navigation.ts`
  </verify>
  <done>
- Blockers is first item in Admin group for adminNav
- Blockers is first item in Admin group for internalNav
- No TypeScript errors
- Other nav arrays unchanged
  </done>
</task>

</tasks>

<verification>
- `grep -n "Blockers" lib/navigation.ts` shows Blockers appearing before Metrics in Admin group
- Visual verification: Login as admin, check sidebar shows Blockers first in Admin group
</verification>

<success_criteria>
- Blockers appears as first item in Admin group for both admin and internal roles
- All other navigation items remain in their original positions
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-sidebar-dashboard-polish/05-01-SUMMARY.md`
</output>
