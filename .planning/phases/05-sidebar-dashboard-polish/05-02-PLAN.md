---
phase: 05-sidebar-dashboard-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/dashboard/dfy/page.tsx
autonomous: true

must_haves:
  truths:
    - "DFY dashboard project cards show hill chart progress percentage"
    - "Progress matches the calculateHillChartProgress() calculation"
    - "Projects without deliverables show 0% or appropriate fallback"
  artifacts:
    - path: "app/(dashboard)/dashboard/dfy/page.tsx"
      provides: "DFY dashboard with hill chart progress"
      contains: "calculateHillChartProgress"
  key_links:
    - from: "app/(dashboard)/dashboard/dfy/page.tsx"
      to: "lib/utils/projectProgress.ts"
      via: "import calculateHillChartProgress"
      pattern: "import.*calculateHillChartProgress.*from.*projectProgress"
---

<objective>
Sync DFY dashboard project cards to use hill chart progress instead of deliverable count

Purpose: Progress displayed should match the hill chart view on project detail pages for consistency
Output: DFY dashboard showing accurate hill chart progress percentages
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-sidebar-dashboard-polish/05-RESEARCH.md
@app/(dashboard)/dashboard/dfy/page.tsx
@lib/utils/projectProgress.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import calculateHillChartProgress function</name>
  <files>app/(dashboard)/dashboard/dfy/page.tsx</files>
  <action>
Add import for `calculateHillChartProgress` from `@/lib/utils/projectProgress` at the top of the file.

Add this import after the existing imports (around line 27):
```typescript
import { calculateHillChartProgress } from '@/lib/utils/projectProgress'
```
  </action>
  <verify>Run `grep "calculateHillChartProgress" app/(dashboard)/dashboard/dfy/page.tsx` to confirm import exists</verify>
  <done>Import statement added for calculateHillChartProgress</done>
</task>

<task type="auto">
  <name>Task 2: Replace deliverable count progress with hill chart progress</name>
  <files>app/(dashboard)/dashboard/dfy/page.tsx</files>
  <action>
In the project card rendering section (around lines 254-256), replace the deliverable count progress calculation with hill chart progress.

Current code:
```typescript
const done = project.deliverables?.filter(d => d.status === 'done').length || 0
const total = project.deliverables?.length || 0
const progress = total > 0 ? Math.round((done / total) * 100) : 0
```

Replace with:
```typescript
const hillProgress = calculateHillChartProgress(project.deliverables)
const progress = hillProgress?.averagePosition || 0
const done = project.deliverables?.filter(d => d.status === 'done').length || 0
const total = project.deliverables?.length || 0
```

This keeps the `done` and `total` variables for the display (which shows "X/Y" count), but uses the hill chart `averagePosition` for the actual progress bar percentage.

The progress bar on line 272 (`<Progress value={progress} ... />`) will now use hill chart position instead of deliverable count percentage.
  </action>
  <verify>
1. Run TypeScript check: `npx tsc --noEmit app/(dashboard)/dashboard/dfy/page.tsx`
2. Verify import is used: `grep -n "hillProgress" app/(dashboard)/dashboard/dfy/page.tsx`
  </verify>
  <done>
- Progress variable uses calculateHillChartProgress().averagePosition
- done/total variables retained for display text
- Progress bar shows hill chart percentage
- No TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- Visual verification: Compare DFY dashboard progress with project detail hill chart
- Projects with deliverables show hill chart average position
- Projects without deliverables show 0%
</verification>

<success_criteria>
- DFY dashboard project cards display hill chart progress percentage
- Progress percentages match those shown on project detail pages
- Deliverable count (X/Y) still displays correctly
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-sidebar-dashboard-polish/05-02-SUMMARY.md`
</output>
