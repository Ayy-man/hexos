---
phase: 21-blocker-queue-redesign
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - features/admin/components/BlockerSidebar.tsx
  - features/admin/components/AdminBlockerQueue.tsx
  - app/(dashboard)/admin/blockers/page.tsx
autonomous: false

must_haves:
  truths:
    - "Clicking a blocker card opens a Sheet sidebar from the right at ~40vw width"
    - "Sidebar has Overview and Conversation tabs using shadcn Tabs with line variant"
    - "Overview tab shows full blocker detail, meta, description, inline resolve (no dialog), inline delete confirm, status transitions, and escalate to DFY"
    - "Conversation tab shows the BlockerConversation chat thread with lazy-loaded comments"
    - "AdminBlockerQueue is a thin orchestrator: filter bar + BlockerCard list + BlockerSidebar"
    - "Filter bar has 3 Select dropdowns (project, priority, status) with default status=Active"
    - "Page fetches ALL blockers (not just active) to support resolved/closed filter"
    - "Selected card has ring highlight, clicking another card switches sidebar content"
    - "Resolve and Comment dialogs are eliminated - replaced by inline sidebar interactions"
  artifacts:
    - path: "features/admin/components/BlockerSidebar.tsx"
      provides: "Sheet sidebar with Overview + Conversation tabs"
      exports: ["BlockerSidebar"]
    - path: "features/admin/components/AdminBlockerQueue.tsx"
      provides: "Rewritten orchestrator: filter bar + card list + sidebar"
      exports: ["AdminBlockerQueue"]
    - path: "app/(dashboard)/admin/blockers/page.tsx"
      provides: "Updated page passing all blockers to queue component"
      contains: "getAllBlockers"
  key_links:
    - from: "features/admin/components/AdminBlockerQueue.tsx"
      to: "features/admin/components/BlockerCard.tsx"
      via: "import and map over filtered blockers"
      pattern: "import.*BlockerCard"
    - from: "features/admin/components/AdminBlockerQueue.tsx"
      to: "features/admin/components/BlockerSidebar.tsx"
      via: "import and render with selected blocker"
      pattern: "import.*BlockerSidebar"
    - from: "features/admin/components/BlockerSidebar.tsx"
      to: "features/admin/components/BlockerConversation.tsx"
      via: "import and render in Conversation tab"
      pattern: "import.*BlockerConversation"
    - from: "app/(dashboard)/admin/blockers/page.tsx"
      to: "lib/api/blockers.ts"
      via: "getAllBlockers() import replacing getAllActiveBlockers()"
      pattern: "getAllBlockers"
    - from: "features/admin/components/BlockerSidebar.tsx"
      to: "features/dev/actions/blockerActions.ts"
      via: "updateBlockerStatusAction, escalateBlockerAction, deleteBlockerAction"
      pattern: "updateBlockerStatusAction|escalateBlockerAction|deleteBlockerAction"
---

<objective>
Assemble the blocker queue redesign: create BlockerSidebar, rewrite AdminBlockerQueue as an orchestrator, update the page to fetch all blockers, and visually verify the complete experience.

Purpose: Wire together the leaf components from Plan 01 into the final UI. The sidebar replaces the resolve/comment dialogs with inline interactions. The queue becomes a scannable card list with a detail pane.

Output: Fully functional blocker queue with card list + slide-over sidebar, replacing the old dense card-dump + dialog approach.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-blocker-queue-redesign/21-CONTEXT.md
@.planning/phases/21-blocker-queue-redesign/21-01-SUMMARY.md
@docs/plans/2026-03-03-blocker-queue-redesign.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From features/admin/components/BlockerCard.tsx (created in Plan 01):
```typescript
interface BlockerCardProps {
  blocker: Blocker
  projectName?: string
  isSelected: boolean
  onClick: () => void
}
export function BlockerCard({ blocker, projectName, isSelected, onClick }: BlockerCardProps)
```

From features/admin/components/BlockerConversation.tsx (created in Plan 01):
```typescript
interface BlockerConversationProps {
  blockerId: string
}
export function BlockerConversation({ blockerId }: BlockerConversationProps)
```

From lib/api/blockers.ts (modified in Plan 01):
```typescript
export async function getAllBlockers(): Promise<Blocker[]>
// Returns ALL blockers including resolved/closed
```

From features/dev/actions/blockerActions.ts (existing):
```typescript
export async function updateBlockerStatusAction(blockerId: string, status: BlockerStatus, notes?: string)
export async function escalateBlockerAction(blockerId: string)
export async function deleteBlockerAction(blockerId: string, projectId?: string)
```

From components/ui/sheet.tsx:
```typescript
// SheetContent supports: side="right", showCloseButton, className
// Default width for right side: w-3/4 sm:max-w-sm — MUST override with className
```

From components/ui/tabs.tsx:
```typescript
// TabsList supports variant="line" | "default"
// TabsContent has data-[state=inactive]:hidden by default
```

From components/ui/avatar.tsx:
```typescript
// Avatar supports size="default" | "sm" | "lg"
// AvatarFallback renders text inside circle
```

Current page (app/(dashboard)/admin/blockers/page.tsx):
```typescript
// Uses: getAllActiveBlockers, getProjects, requireRole(['admin', 'internal'])
// Renders: stat cards (New, Acknowledged, In Progress, Critical/High) + critical alert + AdminBlockerQueue
// Stats are computed from the blockers array
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlockerSidebar, rewrite AdminBlockerQueue, update page data fetching</name>
  <files>features/admin/components/BlockerSidebar.tsx, features/admin/components/AdminBlockerQueue.tsx, app/(dashboard)/admin/blockers/page.tsx</files>
  <action>
**A. Create `features/admin/components/BlockerSidebar.tsx`** — `'use client'` component:

Props: `{ blocker: Blocker | null, onClose: () => void }`

Structure:
- Sheet (open={!!blocker}, onOpenChange triggers onClose when closed)
- SheetContent: side="right", className MUST override default width: `"w-full sm:max-w-[40vw] sm:min-w-[400px] p-0 flex flex-col"` (the default is sm:max-w-sm which is too narrow)
- SheetHeader: priority badge + status badge (with icon) + escalated badge (if applicable), then SheetTitle with blocker title. SheetDescription with sr-only text for accessibility.
- Tabs (defaultValue="overview") with TabsList variant="line":
  - "Overview" tab trigger
  - "Conversation" tab trigger (show comment count badge if > 0)

Overview TabsContent:
- Meta section: reporter avatar + name + "reported" + relative time, project name, deliverable title (if any), acknowledged time (if any)
- Separator
- Description (or "No description" italic placeholder)
- Resolution section (if resolved/closed): resolution notes + resolver name + resolved time
- Separator
- Inline resolve: When "Resolve" clicked, show Textarea for resolution notes + "Confirm Resolve" + "Cancel" buttons (NO dialog). Use local state `showResolveInput`.
- Inline delete: When "Delete" clicked, show red warning box with "Delete this blocker permanently?" + "Delete" (destructive) + "Cancel" buttons (NO dialog). Use local state `showDeleteConfirm`.
- Status transition buttons (only shown when not resolved/closed and not showing resolve input):
  - reported -> Acknowledge button
  - acknowledged -> Start Working button
  - acknowledged or in_progress -> Resolve button (shows inline input)
  - Not escalated -> Escalate to DFY button
  - Delete button (ghost, text-ghost hover:text-signal-bad)
- Reset showResolveInput/showDeleteConfirm/resolutionNotes when blocker changes (use pattern from PRD: compare prevBlockerId via state)

Server actions: updateBlockerStatusAction, escalateBlockerAction, deleteBlockerAction (all existing). Use useTransition for pending state. router.refresh() on success. toast.success/error for feedback.

Conversation TabsContent:
- Render `<BlockerConversation blockerId={blocker.id} />` (from Plan 01)
- Use `className="flex-1 min-h-0 -mx-5"` to let it fill available height and extend to sheet edges

Design tokens: Use brand tokens (signal-bad-dim, accent-dim, signal-good-dim, bg-bg-hover, text-text-primary, etc.) NOT raw Tailwind colors. Reference the PRD Task 3 for near-complete code.

Priority badge config: critical=`bg-signal-bad-dim text-signal-bad`, high=`bg-orange-500/10 text-orange-400`, medium=`bg-signal-warn-dim text-signal-warn`, low=`bg-bg-hover text-text-tertiary` — all `border-transparent`

Status badge config: Same as BlockerCard (New/Acknowledged/In Progress/Resolved/Closed) with icons (AlertTriangle/Clock/Play/Check/Check).

**B. Rewrite `features/admin/components/AdminBlockerQueue.tsx`** — full rewrite as thin orchestrator:

Props: `{ blockers: Blocker[], projects: Project[] }` (same interface as current)

State: filterProject, filterPriority, filterStatus (default "active"), selectedBlocker (Blocker | null)

Filtering logic (same as current but simplified):
- filterProject: match project_id
- filterPriority: match priority
- filterStatus: "active" excludes resolved/closed, "all" shows everything, specific status matches exact

Sorting: priority order (critical=0, high=1, medium=2, low=3) then oldest first by created_at

Keep selected blocker in sync: `const selectedBlockerData = selectedBlocker ? blockers.find(b => b.id === selectedBlocker.id) ?? null : null` — this ensures the sidebar shows fresh data after router.refresh()

Render:
1. Filter bar: horizontal layout with Filter icon + 3 Select dropdowns (project w-[160px], priority w-[130px], status w-[130px]), all h-8 text-xs. NO Card wrapper — just a flex row with gap-3.
2. Card list: `space-y-2` of BlockerCard components, each with onClick that sets selectedBlocker. Empty state: Check icon + "No blockers matching your filters" message.
3. BlockerSidebar: rendered with selectedBlockerData and onClose that sets selectedBlocker to null.

Remove ALL dialog-related code (Dialog imports, resolveDialog state, commentDialog state, handleResolve, handleAddComment, etc.). Remove ALL action handlers (they moved to BlockerSidebar). Remove Card/CardContent/CardHeader/CardTitle imports. Remove Collapsible imports. This is a CLEAN rewrite, not a patch.

**C. Update `app/(dashboard)/admin/blockers/page.tsx`**:

- Change import from `getAllActiveBlockers` to `getAllBlockers` (from `@/lib/api/blockers`)
- In the Promise.all, call `getAllBlockers()` instead of `getAllActiveBlockers()`
- Compute stats from the full array by filtering to active: `const activeBlockers = allBlockers.filter(b => !['resolved', 'closed'].includes(b.status))`
- Use `activeBlockers` for stat counts (reported, acknowledged, inProgress, critical, high)
- Pass `allBlockers` (full set) to `<AdminBlockerQueue blockers={allBlockers} projects={projects} />`
- Keep everything else the same (stat cards, critical alert, header)
  </action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library/hexos-main" && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>BlockerSidebar.tsx created with Sheet + Overview/Conversation tabs, inline resolve/delete, status transitions. AdminBlockerQueue.tsx rewritten as filter bar + card list + sidebar (no dialogs). Page fetches all blockers. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of blocker queue redesign</name>
  <files>features/admin/components/BlockerSidebar.tsx, features/admin/components/AdminBlockerQueue.tsx, app/(dashboard)/admin/blockers/page.tsx</files>
  <action>Human verifies the complete blocker queue redesign visually and functionally. Run `pnpm dev` and navigate to `/admin/blockers`. If issues are found, fix them before marking complete.</action>
  <verify>
    <automated>cd "/Users/aymanbaig/Desktop/Manual Library/hexos-main" && npx tsc --noEmit --pretty 2>&1 | head -5</automated>
  </verify>
  <done>User confirms the blocker queue redesign looks correct and all interactions work: card list, sidebar open/close, overview tab actions, conversation tab messaging, filters.</done>
  <what-built>Complete blocker queue redesign: minimal card list with priority color bars, slide-over sidebar with Overview (detail + actions) and Conversation (chat thread) tabs, inline resolve/delete, filter bar with project/priority/status dropdowns.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` and navigate to `/admin/blockers`
    2. Verify stat cards still render correctly at the top
    3. Verify filter bar shows 3 dropdowns (project, priority, status) in a compact horizontal layout
    4. Verify blocker cards show: left-edge priority color bar, title, description preview (1 line), status badge + project + time + comment count + reporter
    5. Click a card — verify Sheet sidebar opens from the right (~40% viewport width)
    6. Verify selected card has a ring highlight
    7. In the sidebar Overview tab:
       - Check priority + status badges in header
       - Check meta section (reporter, project, deliverable, ack time)
       - Check full description is shown
       - Test status transition buttons (Acknowledge, Start Working)
       - Click "Resolve" — verify inline textarea appears (NO dialog)
       - Click "Delete" — verify inline red confirmation box appears (NO dialog)
       - Test "Escalate to DFY" button
    8. Switch to Conversation tab:
       - Verify chat thread loads (or shows "No messages yet" if empty)
       - Send a message with Enter key — verify it appears immediately
       - Hover over own message — verify edit/delete icons appear
       - Test Shift+Enter for newline in composer
    9. Click a different card — verify sidebar updates to new blocker
    10. Close the sidebar — verify card deselects
    11. Change status filter to "All" — verify resolved/closed blockers appear
    12. Change status filter to "Resolved" — verify only resolved blockers show
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- `/admin/blockers` page loads without errors
- No Dialog components remain in AdminBlockerQueue
- Resolve action works inline in sidebar (no dialog)
- Comment sending works via Conversation tab (no dialog)
- Filter defaults to "Active" status on page load
- Selecting "All" status shows resolved/closed blockers
</verification>

<success_criteria>
- Dense card-dump replaced with scannable minimal cards
- Resolve dialog eliminated — replaced by inline textarea in sidebar
- Comment dialog eliminated — replaced by chat-like Conversation tab
- Sheet sidebar opens at ~40vw with Overview + Conversation tabs
- All existing actions work: acknowledge, start working, resolve, escalate, delete, comment
- Stats cards still accurate (computed from active subset of all blockers)
</success_criteria>

<output>
After completion, create `.planning/phases/21-blocker-queue-redesign/21-02-SUMMARY.md`
</output>
