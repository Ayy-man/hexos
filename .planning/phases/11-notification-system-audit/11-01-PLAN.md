---
phase: 11-notification-system-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260119000001_notification_toast_tracking.sql
  - lib/api/notifications.ts
  - lib/api/notifications-utils.ts
autonomous: true

must_haves:
  truths:
    - "Notifications table has shown_as_toast_at column"
    - "Server can query notifications that have never been shown as toast"
    - "Server can mark notifications as toast-shown"
  artifacts:
    - path: "supabase/migrations/20260119000001_notification_toast_tracking.sql"
      provides: "Database column for toast tracking"
      contains: "shown_as_toast_at"
    - path: "lib/api/notifications.ts"
      provides: "Server-side toast tracking functions"
      exports: ["getUnshownToastNotifications", "markAsToastShown"]
    - path: "lib/api/notifications-utils.ts"
      provides: "Updated Notification type with shown_as_toast_at"
      contains: "shown_as_toast_at"
  key_links:
    - from: "lib/api/notifications.ts"
      to: "supabase notifications table"
      via: "getUnshownToastNotifications query"
      pattern: "shown_as_toast_at.*null"
---

<objective>
Add database infrastructure for toast notification deduplication.

Purpose: Enable tracking of which notifications have been shown as pop-up toasts, preventing the same notification from appearing as a toast multiple times across page loads/navigation.

Output: Migration file adding `shown_as_toast_at` column, updated TypeScript types, and server-side helper functions for querying/updating toast state.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-notification-system-audit/11-RESEARCH.md

Key files:
@lib/api/notifications.ts
@lib/api/notifications-utils.ts
@supabase/migrations/20260107000001_dev_experience_foundation.sql (notifications table definition)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for shown_as_toast_at column</name>
  <files>supabase/migrations/20260119000001_notification_toast_tracking.sql</files>
  <action>
Create a new Supabase migration that:

1. Adds `shown_as_toast_at TIMESTAMPTZ` column to the notifications table (nullable, NULL means never shown as toast)

2. Creates a partial index for efficient querying of notifications that haven't been shown as toast:
   ```sql
   CREATE INDEX idx_notifications_unshown_toast
   ON notifications(user_id, created_at DESC)
   WHERE shown_as_toast_at IS NULL AND read_at IS NULL;
   ```

3. Add a comment explaining the column's purpose

DO NOT modify existing columns or constraints.
  </action>
  <verify>
Run `cat supabase/migrations/20260119000001_notification_toast_tracking.sql` to verify:
- ALTER TABLE adds shown_as_toast_at column
- Index created for efficient unshown toast queries
  </verify>
  <done>Migration file exists with column addition and index creation</done>
</task>

<task type="auto">
  <name>Task 2: Update Notification type and add server functions</name>
  <files>lib/api/notifications-utils.ts, lib/api/notifications.ts</files>
  <action>
**In lib/api/notifications-utils.ts:**

Add `shown_as_toast_at: string | null` to the Notification interface (after `read_at`).

**In lib/api/notifications.ts:**

Add two new exported functions:

1. `getUnshownToastNotifications(limit = 5, minutesWindow = 5)`:
   - Queries notifications where:
     - `user_id` = current user
     - `read_at IS NULL` (unread)
     - `shown_as_toast_at IS NULL` (never shown as toast)
     - `created_at` >= (now - minutesWindow minutes)
   - Orders by created_at DESC
   - Limits to `limit` results
   - Returns Notification[] with actor and project relations
   - Uses the existing `normalizeNotificationRelations` helper

2. `markAsToastShown(notificationIds: string[])`:
   - Updates notifications with given IDs
   - Sets `shown_as_toast_at` to current timestamp
   - Returns void (fire-and-forget pattern)

Both functions should use `createClient` from '@/lib/supabase/server'.

Reference existing patterns in the file (e.g., `getMyNotifications`).
  </action>
  <verify>
Run TypeScript check: `npx tsc --noEmit lib/api/notifications.ts lib/api/notifications-utils.ts`
Verify exports: `grep -E "export (async function|function) (getUnshownToastNotifications|markAsToastShown)" lib/api/notifications.ts`
  </verify>
  <done>
- Notification interface includes shown_as_toast_at field
- getUnshownToastNotifications function exported
- markAsToastShown function exported
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. Migration file exists at `supabase/migrations/20260119000001_notification_toast_tracking.sql`
2. TypeScript types compile: `npx tsc --noEmit`
3. New functions are exported from notifications.ts
</verification>

<success_criteria>
- [ ] Migration adds shown_as_toast_at column with partial index
- [ ] Notification type updated with shown_as_toast_at field
- [ ] getUnshownToastNotifications queries unread, unshown, recent notifications
- [ ] markAsToastShown updates shown_as_toast_at timestamp for given IDs
- [ ] All TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-notification-system-audit/11-01-SUMMARY.md`
</output>
