---
phase: 03-form-input-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - features/blueprints/components/PricingTiersEditor.tsx
  - features/blueprints/components/BlueprintForm.tsx
autonomous: true

must_haves:
  truths:
    - "Typing 250 in a pricing tier setup/monthly field shows 250, not 0250"
    - "Pressing Enter in features textarea creates a newline instead of submitting form"
    - "Base price field accepts decimal values without leading zero issues"
  artifacts:
    - path: "features/blueprints/components/PricingTiersEditor.tsx"
      provides: "Fixed price inputs and textarea"
      contains: "inputMode=\"decimal\""
    - path: "features/blueprints/components/BlueprintForm.tsx"
      provides: "Fixed base price input"
      contains: "inputMode=\"decimal\""
  key_links:
    - from: "PricingTiersEditor.tsx"
      to: "tier state"
      via: "sanitized onChange handler"
      pattern: "replace.*[^0-9.]"
---

<objective>
Fix the critical blueprint form input issues: leading zero problem on pricing tier inputs, Enter key not working in features textarea, and base price input step issue.

Purpose: Users reported that typing in pricing tier fields shows "0250" instead of "250" when the initial value is 0. The textarea for features also doesn't allow Enter for newlines. These are the most user-visible form input bugs.

Output: Working blueprint pricing forms with proper number input behavior and textarea newline support.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-form-input-fixes/03-RESEARCH.md
@features/inquiries/components/QuickPricingEditor.tsx (proven pattern to follow)
@features/blueprints/components/PricingTiersEditor.tsx
@features/blueprints/components/BlueprintForm.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix PricingTiersEditor price inputs and textarea</name>
  <files>features/blueprints/components/PricingTiersEditor.tsx</files>
  <action>
Convert both setup_price and monthly_price inputs from type="number" to the proven text+inputMode pattern from QuickPricingEditor.tsx:

1. For setup_price input (lines 96-106):
   - Change type="number" to type="text"
   - Add inputMode="decimal"
   - Change value from `{tier.setup_price}` to `{tier.setup_price === 0 ? '' : tier.setup_price.toString()}`
   - Update onChange to sanitize input: `const sanitized = e.target.value.replace(/[^0-9.]/g, ''); updateTier(index, { setup_price: sanitized ? parseFloat(sanitized) : 0 })`
   - Remove min="0" (not valid for type="text")

2. For monthly_price input (lines 109-118):
   - Apply same changes as setup_price

3. For features Textarea (lines 123-129):
   - Add onKeyDown handler: `onKeyDown={(e) => { if (e.key === 'Enter') { e.stopPropagation() } }}`
   - This allows Enter to create newlines instead of submitting parent form

DO NOT change the tier.name input - it's a text field that works correctly.
DO NOT import any new libraries - use native browser features only.
  </action>
  <verify>
1. Open /blueprints/new in browser
2. Add a pricing tier
3. Type "250" in setup price field - should show "250" not "0250"
4. Type "100" in monthly price field - should show "100" not "0100"
5. In features textarea, press Enter - should create new line, not submit form
6. Type letters in price fields - should be blocked (only numbers and decimal allowed)
  </verify>
  <done>Price inputs accept clean numeric input without leading zero issues. Textarea accepts Enter key for newlines.</done>
</task>

<task type="auto">
  <name>Task 2: Fix BlueprintForm basePrice input</name>
  <files>features/blueprints/components/BlueprintForm.tsx</files>
  <action>
Convert the basePrice input (lines 117-126) from type="number" to text+inputMode pattern:

1. Change type="number" to type="text"
2. Add inputMode="decimal"
3. Remove step="0.01" (not valid for type="text")
4. Remove min="0" (not valid for type="text")
5. The value binding `{basePrice}` is already a string state, so no change needed there
6. Update onChange to sanitize: `onChange={(e) => setBasePrice(e.target.value.replace(/[^0-9.]/g, ''))}`

The estimatedHours input (lines 107-114) should remain type="number" - it's for hours (small integers) and the leading zero issue is less problematic there since users rarely start with 0.
  </action>
  <verify>
1. Open /blueprints/new in browser
2. Type "500" in base price field - should show "500"
3. Type "abc" - letters should be blocked
4. Type "500.50" - should allow decimal
5. Estimated hours field should still work as number input with spinner arrows
  </verify>
  <done>Base price input uses text+inputMode pattern. Estimated hours remains as number input.</done>
</task>

</tasks>

<verification>
After both tasks:
1. Create a new blueprint at /blueprints/new
2. Set base price to 1500 - no leading zero
3. Add a pricing tier with setup price 500, monthly 100 - no leading zeros
4. Add features with Enter key - newlines work
5. Save blueprint - all prices persist correctly
6. Edit the blueprint - prices display correctly on load
</verification>

<success_criteria>
- PricingTiersEditor setup_price and monthly_price use type="text" with inputMode="decimal"
- PricingTiersEditor textarea has onKeyDown stopPropagation for Enter
- BlueprintForm basePrice uses type="text" with inputMode="decimal"
- No regressions in form submission or data persistence
</success_criteria>

<output>
After completion, create `.planning/phases/03-form-input-fixes/03-01-SUMMARY.md`
</output>
