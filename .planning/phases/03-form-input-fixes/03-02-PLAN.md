---
phase: 03-form-input-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/projects/new/page.tsx
  - features/admin/components/metrics/ExpenseLedger.tsx
  - features/admin/components/metrics/InvoiceManagement.tsx
  - features/dev/components/payouts/SubmitPayoutForm.tsx
  - features/finances/components/RetainerManagement.tsx
  - features/inquiries/components/deliverables/CounterOfferDialog.tsx
  - features/inquiries/components/deliverables/DeliverableRow.tsx
  - features/inquiries/components/steps/CustomProposal.tsx
  - features/project-initiation/components/steps/DeliverablesStep.tsx
  - features/projects/components/tabs/ProjectInfoTab.tsx
autonomous: true

must_haves:
  truths:
    - "All currency/price inputs throughout the app use consistent text+inputMode pattern"
    - "Currency inputs do not show leading zeros when typing"
    - "Currency inputs only accept numeric characters and decimal point"
  artifacts:
    - path: "features/admin/components/metrics/InvoiceManagement.tsx"
      provides: "Fixed invoice price inputs"
      contains: "inputMode=\"decimal\""
    - path: "features/projects/components/tabs/ProjectInfoTab.tsx"
      provides: "Fixed project price inputs"
      contains: "inputMode=\"decimal\""
    - path: "features/inquiries/components/steps/CustomProposal.tsx"
      provides: "Fixed proposal budget input"
      contains: "inputMode=\"decimal\""
  key_links:
    - from: "All currency inputs"
      to: "form state"
      via: "sanitized onChange with replace(/[^0-9.]/g, '')"
      pattern: "type=\"text\".*inputMode=\"decimal\""
---

<objective>
Apply the proven text+inputMode pattern to all HIGH priority currency/price inputs across the application for consistency.

Purpose: The research identified 12 high-priority currency fields using type="number" that have the same leading zero issue as PricingTiersEditor. This plan fixes them all using the pattern already proven in QuickPricingEditor.tsx.

Output: Consistent currency input behavior across the entire application.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-form-input-fixes/03-RESEARCH.md
@features/inquiries/components/QuickPricingEditor.tsx (proven pattern to follow)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix currency inputs in admin and finance features</name>
  <files>
    features/admin/components/metrics/ExpenseLedger.tsx
    features/admin/components/metrics/InvoiceManagement.tsx
    features/dev/components/payouts/SubmitPayoutForm.tsx
    features/finances/components/RetainerManagement.tsx
  </files>
  <action>
For each file, find type="number" inputs that handle currency/price values and convert them:

**ExpenseLedger.tsx** (line ~402):
- Find the amount input
- Change type="number" to type="text"
- Add inputMode="decimal"
- Remove min attribute if present
- Update onChange to sanitize: `.replace(/[^0-9.]/g, '')`

**InvoiceManagement.tsx** (lines ~672, ~687, ~743):
- Find qty, price, and tax rate inputs
- For price inputs: Apply text+inputMode pattern
- For qty input: Keep as type="number" (small integers are fine)
- For tax rate: Apply text+inputMode pattern (percentages should be consistent)

**SubmitPayoutForm.tsx** (line ~237):
- Find the payout amount input
- Apply text+inputMode pattern

**RetainerManagement.tsx** (line ~455):
- Find the retainer amount input
- Apply text+inputMode pattern

For each conversion:
1. type="number" -> type="text"
2. Add inputMode="decimal"
3. Remove min, max, step attributes (not valid for type="text")
4. Wrap onChange value in `.replace(/[^0-9.]/g, '')`

DO NOT change qty/count fields that are small integers.
DO NOT change hour/day fields (covered by LOW priority, kept as type="number").
  </action>
  <verify>
1. ExpenseLedger: Add expense, type amount - no leading zero
2. InvoiceManagement: Create invoice line item, type price - no leading zero
3. SubmitPayoutForm: Enter payout amount - no leading zero
4. RetainerManagement: Set retainer amount - no leading zero
5. All fields reject non-numeric characters
  </verify>
  <done>Admin and finance currency inputs use consistent text+inputMode pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Fix currency inputs in project and inquiry features</name>
  <files>
    app/(dashboard)/projects/new/page.tsx
    features/inquiries/components/deliverables/CounterOfferDialog.tsx
    features/inquiries/components/deliverables/DeliverableRow.tsx
    features/inquiries/components/steps/CustomProposal.tsx
    features/project-initiation/components/steps/DeliverablesStep.tsx
    features/projects/components/tabs/ProjectInfoTab.tsx
  </files>
  <action>
For each file, find type="number" inputs that handle currency/price values and convert them:

**app/(dashboard)/projects/new/page.tsx** (line ~209):
- Find client price input
- Apply text+inputMode pattern

**CounterOfferDialog.tsx** (line ~147):
- Find counter price input
- Apply text+inputMode pattern

**DeliverableRow.tsx** (line ~256):
- Find deliverable price input
- Apply text+inputMode pattern

**CustomProposal.tsx** (line ~364):
- Find budget amount input
- Apply text+inputMode pattern

**DeliverablesStep.tsx** (lines ~610, ~728):
- Find price inputs (may be multiple)
- Apply text+inputMode pattern to price fields
- Keep hour fields as type="number" if present

**ProjectInfoTab.tsx** (lines ~197, ~218, ~237):
- Find the three price fields (client_price, hexona_price, dev_cost)
- Apply text+inputMode pattern to all three

For each conversion:
1. type="number" -> type="text"
2. Add inputMode="decimal"
3. Remove min, max, step attributes
4. Wrap onChange value in `.replace(/[^0-9.]/g, '')`

Be careful to preserve existing value binding logic - only modify the type, inputMode, and sanitization.
  </action>
  <verify>
1. projects/new: Enter client price - no leading zero
2. CounterOfferDialog: Enter counter offer - no leading zero
3. DeliverableRow: Edit deliverable price - no leading zero
4. CustomProposal: Enter budget - no leading zero
5. DeliverablesStep: Add deliverable with price - no leading zero
6. ProjectInfoTab: Edit all three price fields - no leading zeros
7. All fields reject non-numeric characters
  </verify>
  <done>Project and inquiry currency inputs use consistent text+inputMode pattern.</done>
</task>

</tasks>

<verification>
After both tasks, spot check across the app:
1. Create new project - client price input works correctly
2. Create custom proposal - budget input works correctly
3. Edit project info tab prices - all three fields work correctly
4. Add expense in ledger - amount works correctly
5. No TypeScript errors from the changes
6. Forms still submit and persist data correctly
</verification>

<success_criteria>
- All 10 files with HIGH priority currency inputs use text+inputMode pattern
- No type="number" remaining on currency/price inputs (excluding hours, qty, percentages where appropriate)
- Consistent sanitization pattern: `.replace(/[^0-9.]/g, '')`
- No regressions in form functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-form-input-fixes/03-02-SUMMARY.md`
</output>
