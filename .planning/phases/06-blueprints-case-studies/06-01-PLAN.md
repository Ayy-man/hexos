---
phase: 06-blueprints-case-studies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260120000001_add_loom_video_support.sql
  - lib/database.types.ts
  - lib/api/blueprints.ts
  - lib/api/case-studies.ts
  - lib/utils/loom.ts
autonomous: true

must_haves:
  truths:
    - "loom_video_url column exists in blueprints table"
    - "loom_video_url column exists in case_studies table"
    - "TypeScript types include loom_video_url field"
    - "API functions handle loom_video_url in CRUD operations"
    - "getCaseStudiesByBlueprintId function returns case studies linked to a blueprint"
  artifacts:
    - path: "supabase/migrations/20260120000001_add_loom_video_support.sql"
      provides: "Database schema changes for Loom support"
      contains: "ALTER TABLE blueprints ADD COLUMN"
    - path: "lib/utils/loom.ts"
      provides: "Loom URL validation and embed URL conversion"
      exports: ["isValidLoomUrl", "extractLoomVideoId", "getLoomEmbedUrl"]
    - path: "lib/api/blueprints.ts"
      provides: "Blueprint API with loom_video_url support"
      contains: "loom_video_url"
    - path: "lib/api/case-studies.ts"
      provides: "Case study API with loom_video_url and getCaseStudiesByBlueprintId"
      exports: ["getCaseStudiesByBlueprintId"]
  key_links:
    - from: "lib/api/blueprints.ts"
      to: "supabase"
      via: "loom_video_url in select/insert/update"
      pattern: "loom_video_url"
    - from: "lib/api/case-studies.ts"
      to: "supabase"
      via: "getCaseStudiesByBlueprintId query"
      pattern: "eq.*blueprint_id"
---

<objective>
Add database schema and API foundation for Loom video support in blueprints and case studies.

Purpose: Establish the data layer that forms must read/write to, and utilities that UI components will use.
Output: Migration file, regenerated types, updated API functions, Loom utility functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-blueprints-case-studies/06-RESEARCH.md
@lib/api/blueprints.ts
@lib/api/case-studies.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration and regenerate types</name>
  <files>supabase/migrations/20260120000001_add_loom_video_support.sql, lib/database.types.ts</files>
  <action>
Create migration file that:
1. Adds `loom_video_url TEXT` column to `blueprints` table
2. Adds `loom_video_url TEXT` column to `case_studies` table
3. Adds comments on columns for documentation

SQL content:
```sql
-- Add Loom video URL support to blueprints and case studies

ALTER TABLE blueprints
ADD COLUMN IF NOT EXISTS loom_video_url TEXT;

COMMENT ON COLUMN blueprints.loom_video_url IS 'Optional Loom video URL for blueprint walkthrough';

ALTER TABLE case_studies
ADD COLUMN IF NOT EXISTS loom_video_url TEXT;

COMMENT ON COLUMN case_studies.loom_video_url IS 'Optional Loom video URL for case study presentation';
```

Then run `pnpm supabase:types` to regenerate TypeScript types.
  </action>
  <verify>
1. Migration file exists at supabase/migrations/20260120000001_add_loom_video_support.sql
2. `grep -q "loom_video_url" lib/database.types.ts` returns match after type regeneration
  </verify>
  <done>
Migration file created with loom_video_url columns for both tables, types regenerated with new fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Loom utility functions</name>
  <files>lib/utils/loom.ts</files>
  <action>
Create new file `lib/utils/loom.ts` with:

1. `LOOM_URL_PATTERN` - Regex to validate Loom URLs (supports share and embed paths, query params like ?t=30)
2. `isValidLoomUrl(url: string): boolean` - Returns true for valid Loom URLs or empty string
3. `extractLoomVideoId(url: string): string | null` - Extracts video ID from URL
4. `getLoomEmbedUrl(shareUrl: string): string` - Converts share URL to embed URL

Pattern: `/^https?:\/\/(www\.)?loom\.com\/(share|embed)\/[a-f0-9-]+(\?.*)?$/i`

Handle edge cases:
- Empty string returns true for isValidLoomUrl (optional field)
- Invalid URL returns null from extractLoomVideoId
- Invalid URL returns empty string from getLoomEmbedUrl
  </action>
  <verify>
File exists and exports all three functions:
`grep -E "export function (isValidLoomUrl|extractLoomVideoId|getLoomEmbedUrl)" lib/utils/loom.ts`
  </verify>
  <done>
Loom utility file created with URL validation and embed URL conversion functions.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update API layer for Loom support and related case studies</name>
  <files>lib/api/blueprints.ts, lib/api/case-studies.ts</files>
  <action>
Update lib/api/blueprints.ts:
1. Add `loom_video_url: string | null` to Blueprint interface
2. Add `loom_video_url?: string` to CreateBlueprintInput interface
3. Add `loom_video_url?: string | null` to UpdateBlueprintInput interface
4. Add loom_video_url to getBlueprints select: `'id, name, description, base_price, estimated_hours, tags, status, icon, pricing_tiers, loom_video_url'`
5. Add loom_video_url handling in createBlueprint insert
6. Add loom_video_url handling in updateBlueprint (if input.loom_video_url !== undefined)
7. Add loom_video_url to duplicateBlueprint

Update lib/api/case-studies.ts:
1. Add `loom_video_url: string | null` to CaseStudy interface
2. Add `loom_video_url?: string` to CreateCaseStudyInput interface
3. Add `loom_video_url?: string | null` to UpdateCaseStudyInput interface
4. Add loom_video_url to getCaseStudies select
5. Add loom_video_url handling in createCaseStudy insert
6. Add loom_video_url handling in updateCaseStudy
7. Add loom_video_url to duplicateCaseStudy
8. Add new function getCaseStudiesByBlueprintId(blueprintId: string):
   - Select: id, name, description, icon, image_url, client_name, industry
   - Filter: blueprint_id = blueprintId, status = 'published'
   - Order: created_at descending
   - Return: data || []
  </action>
  <verify>
1. `grep -q "loom_video_url" lib/api/blueprints.ts` returns match
2. `grep -q "getCaseStudiesByBlueprintId" lib/api/case-studies.ts` returns match
3. `pnpm tsc --noEmit` passes (no type errors)
  </verify>
  <done>
API layer updated with loom_video_url support in both blueprints and case-studies, getCaseStudiesByBlueprintId function added.
  </done>
</task>

</tasks>

<verification>
1. Migration file exists with correct SQL
2. Types regenerated with loom_video_url in both Blueprint and CaseStudy types
3. Loom utility functions exported and working
4. API functions handle loom_video_url in all CRUD operations
5. getCaseStudiesByBlueprintId function exists and returns case studies
6. `pnpm tsc --noEmit` passes
</verification>

<success_criteria>
- Database migration ready to deploy
- TypeScript types include loom_video_url
- API layer supports loom_video_url in all operations
- getCaseStudiesByBlueprintId available for querying related case studies
- Loom URL utilities ready for UI components
</success_criteria>

<output>
After completion, create `.planning/phases/06-blueprints-case-studies/06-01-SUMMARY.md`
</output>
