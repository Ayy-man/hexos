---
phase: 06-blueprints-case-studies
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - features/blueprints/components/RelatedCaseStudies.tsx
  - app/(dashboard)/blueprints/[id]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Blueprint detail page shows Loom video when URL exists"
    - "Blueprint detail page shows related case studies section when case studies exist"
    - "Related case studies section is hidden when no linked case studies"
    - "Clicking related case study navigates to case study detail page"
  artifacts:
    - path: "features/blueprints/components/RelatedCaseStudies.tsx"
      provides: "Component to display case studies linked to a blueprint"
      exports: ["RelatedCaseStudies"]
      min_lines: 30
    - path: "app/(dashboard)/blueprints/[id]/page.tsx"
      provides: "Blueprint detail page with Loom embed and related case studies"
      contains: "RelatedCaseStudies"
  key_links:
    - from: "app/(dashboard)/blueprints/[id]/page.tsx"
      to: "lib/api/case-studies.ts"
      via: "getCaseStudiesByBlueprintId import and call"
      pattern: "getCaseStudiesByBlueprintId"
    - from: "app/(dashboard)/blueprints/[id]/page.tsx"
      to: "features/blueprints/components/LoomVideoEmbed.tsx"
      via: "LoomVideoEmbed component usage"
      pattern: "<LoomVideoEmbed"
    - from: "app/(dashboard)/blueprints/[id]/page.tsx"
      to: "features/blueprints/components/RelatedCaseStudies.tsx"
      via: "RelatedCaseStudies component usage"
      pattern: "<RelatedCaseStudies"
---

<objective>
Create RelatedCaseStudies component and integrate Loom video embed and related case studies into blueprint detail page.

Purpose: Complete the bidirectional relationship display - blueprints show their related case studies, and both show Loom videos.
Output: RelatedCaseStudies component, updated blueprint detail page with Loom embed and related case studies section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-blueprints-case-studies/06-RESEARCH.md
@.planning/phases/06-blueprints-case-studies/06-01-SUMMARY.md
@.planning/phases/06-blueprints-case-studies/06-02-SUMMARY.md
@app/(dashboard)/blueprints/[id]/page.tsx
@lib/api/case-studies.ts
@features/blueprints/components/LoomVideoEmbed.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RelatedCaseStudies component</name>
  <files>features/blueprints/components/RelatedCaseStudies.tsx</files>
  <action>
Create new file `features/blueprints/components/RelatedCaseStudies.tsx`:

Interface for props:
```typescript
interface RelatedCaseStudy {
  id: string
  name: string
  description: string | null
  icon: string | null
  image_url: string | null
  client_name: string | null
  industry: string | null
}

interface RelatedCaseStudiesProps {
  caseStudies: RelatedCaseStudy[]
}
```

Component logic:
1. If caseStudies.length === 0, return null (don't show empty section)
2. Render Card with:
   - CardHeader: CardTitle "Related Case Studies" (text-base)
   - CardContent: space-y-3 with list of case study links

Each case study link:
- Link component wrapping entire item (href=`/case-studies/${study.id}`)
- Block layout with `p-3 rounded-lg border hover:bg-muted/50 transition-colors`
- Flex row with:
  - Icon (if exists): `text-xl shrink-0`
  - Content div (flex-1 min-w-0):
    - Name: `font-medium truncate`
    - Subtitle (if client_name): `text-sm text-muted-foreground truncate` showing client_name and industry
  - ArrowRight icon: `h-4 w-4 text-muted-foreground shrink-0`

Imports needed:
- Link from 'next/link'
- Card, CardContent, CardHeader, CardTitle from '@/components/ui/card'
- ArrowRight from 'lucide-react'

Export the component as named export.
  </action>
  <verify>
1. File exists at features/blueprints/components/RelatedCaseStudies.tsx
2. `grep -q "RelatedCaseStudies" features/blueprints/components/RelatedCaseStudies.tsx` returns match
3. `grep -q "/case-studies/" features/blueprints/components/RelatedCaseStudies.tsx` returns match (link href)
  </verify>
  <done>
RelatedCaseStudies component created with graceful empty state handling and clickable case study cards.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate Loom embed and RelatedCaseStudies into blueprint detail page</name>
  <files>app/(dashboard)/blueprints/[id]/page.tsx</files>
  <action>
Update app/(dashboard)/blueprints/[id]/page.tsx:

1. Add imports:
   - `import { getCaseStudiesByBlueprintId } from '@/lib/api/case-studies'`
   - `import { LoomVideoEmbed } from '@/features/blueprints/components/LoomVideoEmbed'`
   - `import { RelatedCaseStudies } from '@/features/blueprints/components/RelatedCaseStudies'`

2. After `getBlueprint(id)` call, add parallel fetch for related case studies:
   ```typescript
   const relatedCaseStudies = await getCaseStudiesByBlueprintId(id)
   ```

3. Add Loom video section in the main content area (lg:col-span-2), BEFORE BlueprintContentSection:
   ```tsx
   {/* Loom Video */}
   {blueprint.loom_video_url && (
     <Card>
       <CardHeader>
         <CardTitle>Video Walkthrough</CardTitle>
       </CardHeader>
       <CardContent>
         <LoomVideoEmbed
           url={blueprint.loom_video_url}
           title={`${blueprint.name} walkthrough`}
         />
       </CardContent>
     </Card>
   )}
   ```

4. Add RelatedCaseStudies in the sidebar section (space-y-4 div), AFTER the Info Card and BEFORE the Back Link:
   ```tsx
   {/* Related Case Studies */}
   <RelatedCaseStudies caseStudies={relatedCaseStudies} />
   ```

Note: The component handles empty state internally (returns null when no case studies), so no conditional wrapper needed.
  </action>
  <verify>
1. `grep -q "getCaseStudiesByBlueprintId" app/(dashboard)/blueprints/[id]/page.tsx` returns match
2. `grep -q "LoomVideoEmbed" app/(dashboard)/blueprints/[id]/page.tsx` returns match
3. `grep -q "RelatedCaseStudies" app/(dashboard)/blueprints/[id]/page.tsx` returns match
4. `grep -q "loom_video_url" app/(dashboard)/blueprints/[id]/page.tsx` returns match
5. `pnpm tsc --noEmit` passes
  </verify>
  <done>
Blueprint detail page now shows Loom video (when exists) and related case studies section (when case studies linked).
  </done>
</task>

</tasks>

<verification>
1. RelatedCaseStudies component created with proper empty state handling
2. Blueprint detail page fetches related case studies
3. Loom video displays when blueprint has loom_video_url
4. Related case studies display in sidebar when case studies exist
5. Clicking case study card navigates to case study detail page
6. `pnpm tsc --noEmit` passes
7. `pnpm build` succeeds
</verification>

<success_criteria>
- Blueprint with Loom URL shows embedded video
- Blueprint with linked case studies shows "Related Case Studies" section in sidebar
- Blueprint without Loom URL shows no video section (no empty card)
- Blueprint without linked case studies shows no related section (no empty card)
- Case study cards are clickable and navigate correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-blueprints-case-studies/06-03-SUMMARY.md`
</output>
