---
phase: 16-notification-coverage-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260222000001_sync_notification_type_enum.sql
  - lib/api/notification-helpers.ts
  - features/projects/actions/extensionActions.ts
  - features/projects/actions/scopeActions.ts
  - app/api/webhooks/stripe/route.ts
autonomous: true
requirements:
  - NOTIF-06
  - NOTIF-07

must_haves:
  truths:
    - "All 47 NotificationType values exist in the DB notification_type enum"
    - "scopeActions.ts no longer uses 'as never' casts for notification types"
    - "extensionActions.ts uses createNotification() instead of raw insert"
    - "Stripe webhook uses createNotification() instead of raw insert"
    - "notifyAdmins() helper creates notifications for all admin/internal users"
    - "notifyProjectStakeholders() helper creates notifications for project team"
  artifacts:
    - path: "supabase/migrations/20260222000001_sync_notification_type_enum.sql"
      provides: "DB enum sync with all 47 notification types"
      contains: "ALTER TYPE notification_type ADD VALUE"
    - path: "lib/api/notification-helpers.ts"
      provides: "notifyAdmins and notifyProjectStakeholders helper functions"
      exports: ["notifyAdmins", "notifyProjectStakeholders"]
    - path: "features/projects/actions/extensionActions.ts"
      provides: "Extension notifications using createNotification"
    - path: "features/projects/actions/scopeActions.ts"
      provides: "Scope notifications without as never casts"
  key_links:
    - from: "lib/api/notification-helpers.ts"
      to: "lib/api/notifications.ts"
      via: "imports createNotification"
      pattern: "import.*createNotification.*from.*notifications"
    - from: "lib/api/notification-helpers.ts"
      to: "profiles table"
      via: "queries admin/internal users by role"
      pattern: "in\\('role'.*admin.*internal"
---

<objective>
Sync the database notification_type enum with all 47 TypeScript types, create reusable notification helper functions (notifyAdmins, notifyProjectStakeholders), and fix all inconsistent notification creation patterns (raw inserts and `as never` casts).

Purpose: Foundation required before adding new notifications — enum must accept all types, helpers must exist, and existing patterns must be consistent.
Output: Migration file, notification-helpers.ts module, fixed extensionActions/scopeActions/stripe webhook.
</objective>

<execution_context>
@/Users/aymanbaig/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aymanbaig/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-notification-coverage-overhaul/16-RESEARCH.md
@lib/api/notifications.ts
@lib/api/notifications-utils.ts
@features/projects/actions/extensionActions.ts
@features/projects/actions/scopeActions.ts
@app/api/webhooks/stripe/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration to sync notification_type enum + notification helpers module</name>
  <files>
    supabase/migrations/20260222000001_sync_notification_type_enum.sql
    lib/api/notification-helpers.ts
  </files>
  <action>
**Migration file:** Create idempotent migration that adds ALL missing notification_type enum values. The DB currently has 12 values (project_assigned, blocker_acknowledged, blocker_resolved, blocker_comment, admin_comment, mention, deadline_reminder, status_change, payout_submitted, payout_approved, payout_paid, payout_rejected). The TypeScript NotificationType union in `lib/api/notifications-utils.ts` has 47 types. Add the 35 missing values using the same idempotent pattern from `20260109000015_payouts_dev_workflow.sql`:

```sql
DO $$ BEGIN
  IF NOT EXISTS (SELECT 1 FROM pg_enum WHERE enumlabel = '{value}' AND enumtypid = 'notification_type'::regtype) THEN
    ALTER TYPE notification_type ADD VALUE '{value}';
  END IF;
END$$;
```

Missing values to add (verify against notifications-utils.ts): stage_changed, invoice_sent, invoice_paid, invoice_payment_failed, scope_change_flagged, scope_change_approved, scope_change_rejected, proposal_ready, assigned, requirement_unblocked, testing_ready_dev, testing_ready_admin_int, testing_ready_client, testing_passed, testing_failed, testing_escalated, suggestion_reply, suggestion_status_change, meeting_ready, retainer_check_in_due, retainer_check_in_overdue, retainer_task_assigned, retainer_health_warning, project_completed, project_moved_to_retainer.

Also add new types needed for phase 16 notifications:
- `inquiry_created` — new inquiry submitted
- `proposal_sent` — proposal sent to DFY
- `inquiry_won` — inquiry closed as won
- `inquiry_lost` — inquiry closed as lost
- `escalation_admin` — DFY escalated to admin
- `project_created` — project initiated
- `deliverable_status_change` — deliverable status updated
- `deliverables_confirmed` — deliverables confirmed by admin
- `send_for_signoff` — deliverables sent for client signoff
- `signed_off` — client signed off deliverables
- `check_in_submitted` — dev submitted check-in
- `blocker_raised` — dev raised a blocker
- `meeting_scheduled` — meeting created

**Notification helpers module:** Create `lib/api/notification-helpers.ts` with:

1. `notifyAdmins({ type, title, message?, projectId?, actorId? })` — queries `profiles` table for users with `role IN ('admin', 'internal')`, calls `createNotification()` for each. Import createNotification from `@/lib/api/notifications`. Use `createClient` from `@/lib/supabase/server`. Return void, log errors but don't throw (fire-and-forget for batch notifications).

2. `notifyProjectStakeholders({ projectId, type, title, message?, actorId?, excludeUserId? })` — queries projects table to get dfy_partner_id and project_assignments for dev IDs, plus admin/internal users. Calls createNotification() for each unique user (excluding excludeUserId to avoid self-notification). Use createClient from `@/lib/supabase/server`.

3. `notifyUsers({ userIds, type, title, message?, projectId?, actorId? })` — batch notification for arbitrary user list. Calls createNotification() for each. Fire-and-forget, logs errors.

Follow existing codebase patterns: named exports, JSDoc comments, camelCase functions.
  </action>
  <verify>
Run `npx tsc --noEmit` to confirm no TypeScript errors in the new module. Check the migration SQL is syntactically valid by counting DO blocks matches the number of missing enum values.
  </verify>
  <done>
Migration file adds all 35+ missing enum values idempotently. notification-helpers.ts exports notifyAdmins, notifyProjectStakeholders, notifyUsers with proper types. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix raw insert patterns and as-never casts</name>
  <files>
    features/projects/actions/extensionActions.ts
    features/projects/actions/scopeActions.ts
    app/api/webhooks/stripe/route.ts
    lib/api/notifications-utils.ts
  </files>
  <action>
**extensionActions.ts:** Three locations use raw `supabase.from('notifications').insert()` (around lines 52, 112, 171). Replace each with `createNotification()` imported from `@/lib/api/notifications`. Map existing fields:
- `user_id` → `userId`
- `title` → `title`
- `body` → `message` (note the field rename!)
- Add `type` parameter — use 'status_change' for extension notifications (or add 'extension_requested'/'extension_approved'/'extension_rejected' to the enum if appropriate — use 'status_change' to keep it simple since extensions are project status events)
- Add `projectId` from the projectId variable already in scope
- Add `actorId` from the authenticated user

**scopeActions.ts:** Three locations cast `as never` (around lines 85, 143, 199). Simply remove the `as never` casts since after the migration, `scope_change_flagged`, `scope_change_approved`, and `scope_change_rejected` will be valid DB enum values. The types already exist in the TypeScript NotificationType union.

**Stripe webhook (app/api/webhooks/stripe/route.ts):** Around line 97, replace `supabase2.from('notifications').insert(admins.map(...))` with a loop calling `createNotification()` for each admin. Import createNotification from `@/lib/api/notifications`. This ensures push notifications are sent for payment failures. Note: the webhook uses admin client, so ensure createNotification works in this context — if it uses `createClient()` (server/cookies), consider importing from notification-helpers.ts `notifyAdmins()` instead since it already handles querying admins and creating notifications. Use `notifyAdmins({ type: 'invoice_payment_failed', title: ..., message: ..., projectId: ... })`.

**notifications-utils.ts:** Add any new NotificationType values needed for this phase to the union type:
- `inquiry_created`
- `proposal_sent`
- `inquiry_won`
- `inquiry_lost`
- `escalation_admin`
- `project_created`
- `deliverable_status_change`
- `deliverables_confirmed`
- `send_for_signoff`
- `signed_off`
- `check_in_submitted`
- `blocker_raised`
- `meeting_scheduled`

Add corresponding entries to `getNotificationIcon()` and `getNotificationColor()` switch statements. Also add URL routing for these types in `getNotificationUrl()`.
  </action>
  <verify>
Run `npx tsc --noEmit`. Grep for `as never` in scopeActions.ts — should return zero matches. Grep for `.from('notifications').insert` in extensionActions.ts — should return zero matches. Grep for `.from('notifications').insert` in the stripe webhook — should return zero matches.
  </verify>
  <done>
Zero `as never` casts remain for notification types. Zero raw notification inserts remain in extensionActions.ts and stripe webhook. All notification creation uses createNotification() or notification helpers. New notification types added to TS union with icons, colors, and URL routing.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. `grep -r "as never" features/projects/actions/scopeActions.ts` returns no notification-related matches
3. `grep -r "from('notifications').insert" features/projects/actions/extensionActions.ts` returns no matches
4. `grep -r "from('notifications').insert" app/api/webhooks/stripe/route.ts` returns no matches
5. lib/api/notification-helpers.ts exports notifyAdmins, notifyProjectStakeholders, notifyUsers
6. Migration file contains idempotent ADD VALUE blocks for all missing enum values
</verification>

<success_criteria>
- DB notification_type enum will accept all 47+ notification types after migration runs
- All notification creation in the codebase uses createNotification() (with push) or the new helpers
- No type safety hacks (`as never`) for notification types
- Reusable helpers ready for Plans 02-05 to consume
</success_criteria>

<output>
After completion, create `.planning/phases/16-notification-coverage-overhaul/16-01-SUMMARY.md`
</output>
