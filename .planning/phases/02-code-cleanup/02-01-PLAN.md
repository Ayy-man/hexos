---
phase: 02-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(dashboard)/settings/team/page.tsx
  - app/(dashboard)/admin/time-reports/page.tsx
  - features/admin/components/TimeReportsContent.tsx
  - lib/navigation.ts
  - components/command-palette.tsx
  - components/dynamic-breadcrumb.tsx
  - lib/api/admin-reports.ts
  - features/organizations/actions/invitationActions.ts
  - features/organizations/actions/organizationActions.ts
autonomous: true

must_haves:
  truths:
    - "Settings team page (/settings/team) returns 404"
    - "Time reports page (/admin/time-reports) returns 404"
    - "Navigation shows no Team or Time Reports links for admin/internal roles"
    - "Command palette has no Team Settings or Time Reports entries for admin"
    - "Active team pages still work (/admin/team, /dashboard/dev/settings/team, /dashboard/dfy/settings/team)"
    - "/admin/devs page still works (getAllDevs function intact)"
  artifacts:
    - path: "lib/navigation.ts"
      provides: "Navigation config without dead routes"
      contains: "adminNav"
    - path: "lib/api/admin-reports.ts"
      provides: "getAllDevs function only"
      exports: ["getAllDevs"]
    - path: "components/command-palette.tsx"
      provides: "Command palette without dead routes"
  key_links:
    - from: "/admin/devs/page.tsx"
      to: "lib/api/admin-reports.ts"
      via: "getAllDevs import"
      pattern: "getAllDevs"
---

<objective>
Remove unused placeholder features (team section at /settings/team, time reports section) to reduce maintenance burden and clean up navigation.

Purpose: Eliminate dead code from previous iterations, simplifying the codebase for future maintenance.
Output: Cleaner codebase with removed placeholder pages, updated navigation, and cleaned API functions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-code-cleanup/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused pages and components</name>
  <files>
    app/(dashboard)/settings/team/page.tsx
    app/(dashboard)/admin/time-reports/page.tsx
    features/admin/components/TimeReportsContent.tsx
  </files>
  <action>
Delete the following files completely:
1. `app/(dashboard)/settings/team/page.tsx` - Placeholder team settings page
2. `app/(dashboard)/admin/time-reports/page.tsx` - Unused time reports page
3. `features/admin/components/TimeReportsContent.tsx` - Time reports component

CRITICAL: Do NOT delete:
- `app/(dashboard)/admin/team/` - Active Hexona Team admin page
- `app/(dashboard)/dashboard/dev/settings/team/` - Active dev team settings
- `app/(dashboard)/dashboard/dfy/settings/team/` - Active DFY team settings

After deletion, verify no orphan imports exist by running build.
  </action>
  <verify>
Run `pnpm build` - should succeed with no missing module errors.
Run `ls app/(dashboard)/settings/` - should not contain `team/` directory.
Run `ls app/(dashboard)/admin/` - should not contain `time-reports/` directory.
  </verify>
  <done>
Three files deleted. Build passes. No orphan imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update navigation and UI references</name>
  <files>
    lib/navigation.ts
    components/command-palette.tsx
    components/dynamic-breadcrumb.tsx
  </files>
  <action>
**In lib/navigation.ts:**
1. In `adminNav` Management section, remove: `{ title: 'Team', url: '/settings/team', icon: 'Users' },`
2. In `adminNav` Admin section, remove: `{ title: 'Time Reports', url: '/admin/time-reports', icon: 'Clock' },`
3. In `internalNav` Admin section, remove: `{ title: 'Time Reports', url: '/admin/time-reports', icon: 'Clock' },`

DO NOT REMOVE:
- `{ title: 'Hexona Team', url: '/admin/team', icon: 'Shield' },` in Teams section
- `{ title: 'Team', url: '/dashboard/dev/settings/team', icon: 'Users' },` in devNav
- `{ title: 'Team', url: '/dashboard/dfy/settings/team', icon: 'Users' },` in dfyNav

**In components/command-palette.tsx:**
1. In `getQuickActions()` function, for admin role (around line 200-206):
   - Remove the `nav-admin-settings-team` entry that links to `/settings/team`
2. For admin/internal roles (around line 150-157):
   - Remove the `nav-time-reports` entry that links to `/admin/time-reports`

DO NOT REMOVE:
- `nav-admin-team` entry linking to `/admin/team` (Hexona Team)
- `nav-dfy-settings-team` entry linking to `/dashboard/dfy/settings/team`
- `nav-dev-settings-team` entry linking to `/dashboard/dev/settings/team`

**In components/dynamic-breadcrumb.tsx:**
1. Remove `'/settings/team': 'Team',` from PAGE_TITLES object (around line 32)
  </action>
  <verify>
Run `pnpm build` - should succeed.
Manually verify in navigation.ts that only 3 lines were removed.
Verify command-palette.tsx still has DFY/Dev team entries.
  </verify>
  <done>
Navigation files updated. Admin/internal nav no longer shows Team or Time Reports in removed locations. DFY/Dev team settings and Hexona Team links preserved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Clean up API functions and dead revalidatePath calls</name>
  <files>
    lib/api/admin-reports.ts
    features/organizations/actions/invitationActions.ts
    features/organizations/actions/organizationActions.ts
  </files>
  <action>
**In lib/api/admin-reports.ts:**
Remove the following (keeping ONLY `getAllDevs()` function):
- `DevTimeReport` interface (lines 4-15)
- `TimeEntryWithDetails` interface (lines 17-41)
- `ProjectTimeReport` interface (lines 43-54)
- `getAllTimeEntries()` function (lines 56-69)
- `getDevTimeReports()` function (lines 71-119)
- `getProjectTimeReports()` function (lines 121-168)
- `getTimeSummaryStats()` function (lines 186-203)
- `normalizeTimeEntry()` helper (lines 206-216)

The final file should contain ONLY:
- Import statement for createClient
- `getAllDevs()` function (lines 170-184)

**In features/organizations/actions/invitationActions.ts:**
Remove dead revalidatePath calls that reference the deleted route:
- Line 220: `revalidatePath('/settings/team')` in `inviteTeamMemberAction`
- Line 433: `revalidatePath('/settings/team')` in `revokeInvitationAction`
- Line 462: `revalidatePath('/settings/team')` in `resendInvitationAction`

KEEP all other revalidatePath calls (they reference valid routes).

**In features/organizations/actions/organizationActions.ts:**
Remove dead revalidatePath calls:
- Line 45: `revalidatePath('/settings/team')` in `createOrganizationAction`
- Line 75: `revalidatePath('/settings/team')` in `updateOrganizationAction`
- Line 141: `revalidatePath('/settings/team')` in `updateMemberRoleAction`
- Line 176: `revalidatePath('/settings/team')` in `deactivateMemberAction`
- Line 221: `revalidatePath('/settings/team')` in `reactivateMemberAction`
- Line 322: `revalidatePath('/settings/team')` in `createDevAgencyAction`

KEEP all other revalidatePath calls.
  </action>
  <verify>
Run `pnpm build` - should succeed.
Run `grep -r "getAllDevs" lib/api/admin-reports.ts` - should find the function.
Run `grep -r "TimeReport" lib/api/admin-reports.ts` - should find nothing.
Run `grep -r "/settings/team" features/organizations/` - should find nothing.
  </verify>
  <done>
Admin reports file contains only getAllDevs(). All dead revalidatePath('/settings/team') calls removed. Build passes. /admin/devs page still works.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm build` succeeds with no errors
2. Verify deleted routes return 404:
   - `/settings/team` - should 404
   - `/admin/time-reports` - should 404
3. Verify active routes still work:
   - `/admin/team` - Hexona Team page loads
   - `/dashboard/dev/settings/team` - Dev team settings loads
   - `/dashboard/dfy/settings/team` - DFY team settings loads
   - `/admin/devs` - Developers page loads (uses getAllDevs)
4. Check navigation sidebar - no Team or Time Reports entries in admin Management/Admin sections
5. Check command palette (Cmd+K) - no Team Settings or Time Reports entries
</verification>

<success_criteria>
- Build passes with zero errors
- Three files deleted (settings/team page, time-reports page, TimeReportsContent)
- Navigation updated (3 items removed from adminNav/internalNav)
- Command palette updated (2 entries removed)
- Breadcrumb updated (1 entry removed)
- Admin reports cleaned (only getAllDevs remains)
- Dead revalidatePath calls removed (9 calls across 2 files)
- All active team features still functional
</success_criteria>

<output>
After completion, create `.planning/phases/02-code-cleanup/02-01-SUMMARY.md`
</output>
